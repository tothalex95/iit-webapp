import { TimetableEntry } from "./timetable-entry";

export class Timetable {
    monday: Array<TimetableEntry>;
    tuesday: Array<TimetableEntry>;
    wednesday: Array<TimetableEntry>;
    thursday: Array<TimetableEntry>;
    friday: Array<TimetableEntry>;
}